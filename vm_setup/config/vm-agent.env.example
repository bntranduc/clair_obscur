# Copier vers /etc/clair-obscur/vm-agent.env (connect.sh le remplit automatiquement)

# Mode API (recommandé — pas de credentials AWS)
API_BASE=http://15.236.51.2:8020
VM_API_TOKEN=
SHIP_MODE=api
VM_ID=

# Mode S3 direct (legacy / EC2 avec rôle IAM)
# AWS_REGION=eu-west-3
# AWS_DEFAULT_REGION=eu-west-3
# RAW_LOGS_BUCKET=clair-obscur-<account_id>-raw-logs
# RAW_LOGS_PREFIX=raw/opensearch/logs-raw/
# SHIP_MODE=s3

VM_SHIP_INTERVAL_SEC=60
VM_NGINX_PORT=8080
