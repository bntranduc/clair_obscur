import AlertDetailClient from "@/components/dashboard/AlertDetailClient";

export const metadata = {
  title: "Alerte — CLAIR OBSCUR",
};

export default function AlertDetailPage() {
  return <AlertDetailClient />;
}
