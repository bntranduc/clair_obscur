"""Domain-level log processing (source-agnostic).

Keep this package free of AWS-specific concerns so it can be reused in local
scripts, notebooks, and different runtimes (Lambda/Fargate/SageMaker).
"""

